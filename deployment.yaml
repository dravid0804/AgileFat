apiVersion: apps/v1
kind: Deployment
metadata:
  name: html-demo0

spec:
  replicas: 2

  selector:
    matchLabels:
      app: html-demo0

  template:
    metadata:
      labels:
        app: html-demo0

    spec:
      containers:
        - name: html-demo0
          image: 23mis0036/html-demo0:latest
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: html-demo-service

spec:
  type: NodePort
  selector:
    app: html-demo

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30011
